package com.example.langlab.Elements;

public class BinaryOpFunctionType extends FunctionType {
    public BinaryOpFunctionType(Type returnType, Type left, Type right) {
        super(returnType, left, right);
    }
}
